language: java

jdk:
  - oraclejdk14

os:
  - linux

cache:
  directories:
    - $HOME/.gradle

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew clean install
  - ./gradlew clean test